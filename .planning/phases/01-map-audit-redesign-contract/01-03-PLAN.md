---
phase: 01-map-audit-redesign-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - docs/WORLD_MAP_PHASE1_PROOF.md
autonomous: true
requirements:
  - CONT-01
must_haves:
  truths:
    - Reviewer can run the heading checklist, live-constant table, and git path filter in docs/WORLD_MAP_PHASE1_PROOF.md and see CONT-01 proven (D-25 D-26 D-27 D-28)
    - Both WORLD_MAP_AUDIT.md and WORLD_MAP_REDESIGN_CONTRACT.md exist with RESEARCH-required headings
    - git diff --name-only -- src/world/ is empty; LAYOUT_VERSION remains 2; WORLD_BUILD_SEED remains 0xB35E7E
  artifacts:
    - path: docs/WORLD_MAP_PHASE1_PROOF.md
      provides: Runnable coverage proof for CONT-01
      contains: TOTAL_SCENERY_COUNT
  key_links:
    - from: docs/WORLD_MAP_PHASE1_PROOF.md
      to: docs/WORLD_MAP_AUDIT.md
      via: Live-constant rows must match audit quotes and RESEARCH census
      pattern: TOTAL_SCENERY_COUNT
    - from: docs/WORLD_MAP_PHASE1_PROOF.md
      to: docs/WORLD_MAP_REDESIGN_CONTRACT.md
      via: Camera ID and shout-noun checklist
      pattern: cam_loop_01_camp_clearing
  prohibitions:
    - Do not rewrite docs/WORLD_MAP_FOUNDATION.md
    - Do not edit src/world/ or tests/world_render_validation.*
    - Do not bump LAYOUT_VERSION or WORLD_BUILD_SEED
    - Do not implement cameras or promote wild frames
    - Do not scaffold a web app or tests/map_contract_validation.gd
---

## Phase Goal

**As a** map reviewer, **I want to** run a heading checklist, live-constant table, and git path filter against the two docs, **so that** CONT-01 is proven before Phase 2 touches layout (D-25, D-26, D-27, D-28).

<objective>
Deliver docs/WORLD_MAP_PHASE1_PROOF.md — the runnable coverage/proof slice of CONT-01.

Purpose: A stranger can verify both deliverable docs against RESEARCH census and CONTEXT D-01 through D-28 without re-running Godot or rewriting foundation.md.
Output: docs/WORLD_MAP_PHASE1_PROOF.md only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-map-audit-redesign-contract/01-CONTEXT.md
@.planning/phases/01-map-audit-redesign-contract/01-RESEARCH.md
@.planning/phases/01-map-audit-redesign-contract/01-VALIDATION.md
@docs/WORLD_MAP_AUDIT.md
@docs/WORLD_MAP_REDESIGN_CONTRACT.md
@src/world/resource_scatter_2d.gd
@src/world/world_map_2d.gd
@src/world/world_ambient_scenery_2d.gd
</context>

## Artifacts this phase produces

- docs/WORLD_MAP_PHASE1_PROOF.md (this plan)
- Proof that docs/WORLD_MAP_AUDIT.md and docs/WORLD_MAP_REDESIGN_CONTRACT.md cover CONT-01
- Decision coverage table D-01 through D-28

<tasks>

<task type="auto">
  <name>Task 1: Write heading checklist against both deliverable docs</name>
  <files>docs/WORLD_MAP_PHASE1_PROOF.md</files>
  <read_first>
    docs/WORLD_MAP_AUDIT.md
    docs/WORLD_MAP_REDESIGN_CONTRACT.md
    .planning/phases/01-map-audit-redesign-contract/01-RESEARCH.md
    .planning/phases/01-map-audit-redesign-contract/01-VALIDATION.md
    .planning/phases/01-map-audit-redesign-contract/01-CONTEXT.md
  </read_first>
  <action>
Create docs/WORLD_MAP_PHASE1_PROOF.md as a reviewer-runnable coverage record (CONT-01). Analog: honest method paragraph like ADDONS_DEEP_AUDIT (what this file is: grep evidence; what it is not: a Godot gate, not a foundation rewrite).

H2 headings verbatim:
1. Method
2. Audit heading checklist
3. Contract heading checklist
4. Live-constant table versus RESEARCH census
5. Git path filter
6. Decision coverage D-01 through D-28
7. Cameras specified not implemented
8. Remaining proof boundary

This task fills 1–3 and 6–8. Leave heading 4 and 5 as stubs naming Task 2.

Method: proof is markdown + git. Do not run Godot. Do not rewrite docs/WORLD_MAP_FOUNDATION.md. Do not instantiate BesprenWorldMap2D. Do not create tests/map_contract_validation.gd.

Audit heading checklist — table Heading / Present. Required H2 strings copied from WORLD_MAP_AUDIT.md (must already exist from plan 01):
- Method
- Extents, seed, camp, cell
- Biome seats as-is
- Eight routes and missing spur
- Density tables
- Pocket versus obstacle overlaps
- 1× unreadables
- Capture inventory
- Foundation drift
- What the contract must invent

Contract heading checklist — required H2 from WORLD_MAP_REDESIGN_CONTRACT.md (plan 02):
- Banner
- Camp
- Natures
- Roads
- District jobs and topology
- Five shout nouns
- 30s loop beats
- 10-minute loop
- Salvage and threat pockets
- Exclusion volumes
- Way-home language
- Capture cameras
- Co-authorship and tripwires
- Phase 2 consumer list
- Out of scope

For each row mark Present only after reading the file and finding that exact H2. If a heading is missing, fail this task — fix by editing is forbidden here; report the miss. This plan does not rewrite the two deliverable docs.

Decision coverage table: one row per D-01 through D-28. Columns Decision / Where cited (audit, contract, or both) / One-line locked text. Every D-NN must have a Where-cited that is not empty. Locked text must match CONTEXT, including D-11 five shout nouns and D-12 Metsä threat weenie as the 30s named landmark.

Cameras specified not implemented (D-27): list all fifteen camera_id strings and state that tests/world_render_validation.gd and .tscn are untouched. Shout-noun count is five; east has no shout noun (D-11).

Remaining proof boundary: desktop/headless success does not certify Android; Phase 1 remaining proof is this file plus empty src/world/ diff. Not GPU. Not device.

Do not edit src/, tests/, WORLD_MAP_AUDIT.md, WORLD_MAP_REDESIGN_CONTRACT.md, or WORLD_MAP_FOUNDATION.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('docs/WORLD_MAP_PHASE1_PROOF.md','utf8'); const need=['## Method','## Audit heading checklist','## Contract heading checklist','## Decision coverage D-01 through D-28','## Cameras specified not implemented','D-01','D-11','D-24','D-28','cam_loop_01_camp_clearing','cam_job_metsa_canopy_wall']; const miss=need.filter(s=>!t.includes(s)); if(miss.length){console.error('MISSING '+miss.join(' | ')); process.exit(1);} const audit=fs.readFileSync('docs/WORLD_MAP_AUDIT.md','utf8'); const contract=fs.readFileSync('docs/WORLD_MAP_REDESIGN_CONTRACT.md','utf8'); const aH=['## Method','## Extents, seed, camp, cell','## Eight routes and missing spur','## Density tables','## Pocket versus obstacle overlaps']; const cH=['## Banner','## Camp','## Five shout nouns','## 30s loop beats','## Capture cameras','## Exclusion volumes']; const am=aH.filter(s=>!audit.includes(s)); const cm=cH.filter(s=>!contract.includes(s)); if(am.length||cm.length){console.error('DELIVERABLE_HEADINGS '+am.join('|')+' // '+cm.join('|')); process.exit(1);} console.log('PROOF HEADINGS OK');"</automated>
  </verify>
  <acceptance_criteria>
    - docs/WORLD_MAP_PHASE1_PROOF.md exists with Method, Audit heading checklist, Contract heading checklist, Decision coverage D-01 through D-28
    - Contains D-01, D-11, D-24, D-28, cam_loop_01_camp_clearing, cam_job_metsa_canopy_wall
    - docs/WORLD_MAP_AUDIT.md still contains the required audit H2 headings
    - docs/WORLD_MAP_REDESIGN_CONTRACT.md still contains Banner, Camp, Five shout nouns, 30s loop beats, Capture cameras, Exclusion volumes
  </acceptance_criteria>
  <done>
    Reviewer can tick every required H2 on both deliverable docs and see D-01 through D-28 mapped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill live-constant table and git path-filter evidence</name>
  <files>docs/WORLD_MAP_PHASE1_PROOF.md</files>
  <read_first>
    docs/WORLD_MAP_PHASE1_PROOF.md
    docs/WORLD_MAP_AUDIT.md
    docs/WORLD_MAP_REDESIGN_CONTRACT.md
    src/world/world_map_2d.gd
    src/world/world_ambient_scenery_2d.gd
    src/world/resource_scatter_2d.gd
    tests/existing_wild_atlas_context_validation.gd
    tests/world_render_validation.gd
    .planning/phases/01-map-audit-redesign-contract/01-RESEARCH.md
  </read_first>
  <action>
Fill H2 Live-constant table versus RESEARCH census and H2 Git path filter in docs/WORLD_MAP_PHASE1_PROOF.md (D-26 D-28). Read live .gd files and the audit; do not copy foundation 576/23/150 as live.

Constant table columns: Constant / Live value in .gd / Quoted in WORLD_MAP_AUDIT.md / Matches RESEARCH. Rows required:
- GRID_SIZE 14
- PLAYABLE_HALF_EXTENT 14336.0
- STARTING_CAMP_POSITION (9950, 2400)
- WORLD_BUILD_SEED 0xB35E7E
- TOTAL_SCENERY_COUNT 640
- ZONE_SCENERY_COUNTS forest index 4 = 236
- RESOURCE_COUNT 87
- LAYOUT_VERSION 2
- capture_count 25
- MAXIMUM_UNHOSTABLE_POCKETS 1
- OstariSouthShell at (4850, -3150)
- wilderness pocket 2 Vector4(2600, -2200, 700, 620)
- forbidden_pending_human_visual_veto
- no camp spur today (audit prose vs eight routes, none at camp)
- WorldBackgroundDecor2D.CAMP_POSITION duplicate (9950, 2400) listed on the contract consumer table

Every Matches-RESEARCH cell must be yes. If the audit omitted a row, fail — this plan does not silently invent the quote; stop and name the missing token.

Git path filter section must document the exact commands and expected empty output:
- git diff --name-only -- src/world/
- git diff --name-only -- tests/world_render_validation.gd tests/world_render_validation.tscn
- Confirm LAYOUT_VERSION still 2 by reading src/world/resource_scatter_2d.gd
- Confirm WORLD_BUILD_SEED still 0xB35E7E by reading src/world/world_map_2d.gd
- Confirm TOTAL_SCENERY_COUNT still 640 by reading src/world/world_ambient_scenery_2d.gd
- Allowed paths this phase: docs/*.md and .planning/phases/01-map-audit-redesign-contract/**
- Forbidden even as comments: world_map_2d.gd, world_road_network_2d.gd, world_obstacle_2d.gd, resource_scatter_2d.gd, camera lists in world-render validation

Run those git commands while writing the proof and paste their empty (or docs-only) result. If src/world/ is dirty, fail the task.

Do not edit src/world/, tests/, LAYOUT_VERSION, WORLD_BUILD_SEED, TOTAL_SCENERY_COUNT, WILD_TREE_REGIONS, or WILDERNESS_POCKETS. Do not rewrite WORLD_MAP_FOUNDATION.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const {spawnSync}=require('child_process'); const t=fs.readFileSync('docs/WORLD_MAP_PHASE1_PROOF.md','utf8'); const need=['## Live-constant table versus RESEARCH census','## Git path filter','TOTAL_SCENERY_COUNT','640','9950','0xB35E7E','OstariSouthShell','forbidden_pending_human_visual_veto','LAYOUT_VERSION','87','capture_count']; const miss=need.filter(s=>!t.includes(s)); if(miss.length){console.error('MISSING '+miss.join(' | ')); process.exit(1);} const audit=fs.readFileSync('docs/WORLD_MAP_AUDIT.md','utf8'); const aNeed=['TOTAL_SCENERY_COUNT','9950','0xB35E7E','OstariSouthShell','forbidden_pending_human_visual_veto']; const am=aNeed.filter(s=>!audit.includes(s)); if(am.length){console.error('AUDIT_TOKENS '+am.join('|')); process.exit(1);} const scatter=fs.readFileSync('src/world/resource_scatter_2d.gd','utf8'); if(!/LAYOUT_VERSION\s*=\s*2/.test(scatter)){console.error('LAYOUT_VERSION_MOVED'); process.exit(1);} const map=fs.readFileSync('src/world/world_map_2d.gd','utf8'); if(!map.includes('0xB35E7E')){console.error('SEED_MOVED'); process.exit(1);} const g=spawnSync('git',['diff','--name-only','--','src/world/'],{encoding:'utf8'}); if((g.stdout||'').trim()){console.error('SRC_WORLD_DIRTY '+g.stdout); process.exit(1);} const g2=spawnSync('git',['diff','--name-only','--','tests/world_render_validation.gd','tests/world_render_validation.tscn'],{encoding:'utf8'}); if((g2.stdout||'').trim()){console.error('CAMERAS_IMPLEMENTED '+g2.stdout); process.exit(1);} console.log('PROOF CENSUS OK');"</automated>
    <human-check>Open docs/WORLD_MAP_AUDIT.md and docs/WORLD_MAP_REDESIGN_CONTRACT.md. Confirm a stranger can census the current map and read the locked 30s Metsä loop plus five shout nouns without opening src/. Confirm the proof file's git path filter section matches an empty src/world/ diff.</human-check>
  </verify>
  <acceptance_criteria>
    - docs/WORLD_MAP_PHASE1_PROOF.md contains TOTAL_SCENERY_COUNT, 640, 9950, 0xB35E7E, OstariSouthShell, forbidden_pending_human_visual_veto, LAYOUT_VERSION, 87
    - src/world/resource_scatter_2d.gd still has LAYOUT_VERSION = 2
    - src/world/world_map_2d.gd still contains 0xB35E7E
    - git diff --name-only -- src/world/ prints nothing
    - git diff --name-only -- tests/world_render_validation.gd tests/world_render_validation.tscn prints nothing
  </acceptance_criteria>
  <done>
    CONT-01 is proven: both docs exist with required headings, audit quotes live constants, git path filter on src/world/ is empty, cameras are specified not implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proof markdown → reviewer | A green-looking table that quotes foundation 576/23/150 reintroduces stale authority |
| Proof task → src/world/ | Measuring by editing layout fails success criterion 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | src/world/*.gd | high | mitigate | Proof plan files_modified is docs/WORLD_MAP_PHASE1_PROOF.md only; git path filter must stay empty |
| T-01-02 | Tampering | tests/world_render_validation.* | high | mitigate | Cameras-specified section plus empty diff on those two files |
| T-01-03 | Information disclosure | docs/ secrets | low | mitigate | Proof quotes public constants only |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase |
</threat_model>

<verification>
Run the node automated checks in both tasks. Confirm LAYOUT_VERSION still 2 and WORLD_BUILD_SEED still 0xB35E7E in .gd. Do not run Godot GPU or headless world-map gates.
</verification>

<success_criteria>
A reviewer running the proof file's checklists can confirm CONT-01: live audit, named contract anatomy, empty src/world/ diff, cameras specified not implemented.
</success_criteria>

<output>
Create `.planning/phases/01-map-audit-redesign-contract/01-03-SUMMARY.md` when done
</output>
