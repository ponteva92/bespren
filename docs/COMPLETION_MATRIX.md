# Foundation completion matrix

Moved from CLAUDE.md section 14 on 2026-09-25 and kept current there-after: one row per requirement, the artifact that implements it, the evidence that proves it, and its status. CLAUDE.md keeps only what is open.

| Requirement | Implemented artifact | Current evidence | Status |
|---|---|---|---|
| Comprehensive GDD | `CLAUDE.md` | Architecture, art, scope, budgets, gates | Complete for foundation |
| Strongly typed GDScript | `src/core`, `src/coop`, `src/game`, `src/input`, `src/ui`, `src/visual`, `src/world` | Godot class registration, runtime tests, `untyped_declaration = 2` | Complete for this slice |
| Character-select boot | `StartMenu.tscn` | Heikki/Shane selection and active bounce tween validated | Passing |
| Multitouch controls | `MobileControls.tscn` | Three simultaneous indices, 8-way vectors, 44×44 targets validated | Passing |
| Authority boundary | `CoopSession` | `COOP HARDENING OK (33 checks)` plus fresh two-process ENet movement/action/auto-aim, resource, and tactical broadcasts | Passing on desktop LAN loopback |
| Base refuge visual | `base_core.tscn` | Blender-rendered camp, PointLight2D, pulsing aura/scale glow, and collision tested | Complete |
| Player physics and animation | `network_player.tscn` | Radius-9 CharacterBody2D collision plus velocity-driven Idle/Run tested | Passing |
| HUD command array | `game_hud.tscn` | `148 x 58` dashboard, 50.9% area reduction, recessed icon plates, raised `40 x 29` commands with derived pressed state, three `44 x 44` touch targets, separate wave chip, safe toast; `MOBILE SYSTEMS OK (58)` and seven Mobile/Vulkan HUD captures including a held command | Passing desktop; device validation required |
| Day/night clock | `DayNightCycle` | 450s automatic transition, host command, six warning thresholds, -65%/+35% states | Passing solo and desktop ENet |
| Tier-1 build deck | `StructureCatalog`, `TacticalBuildDeck` | Safe-area top sheet, Towers/Traps/Utilities tabs, eight cards, thumbnails, live resources, remembered selection, three-card pages; `TACTICAL HUD OK (54)` | Passing desktop; device validation required |
| 2D construction | `GridPlacementController2D`, `TacticalBuildSystem`, `PlacedStructure2D` | Eight unique silhouettes, exact 64px X/Y snap, host spending, blocking utilities/towers, and nonblocking ground traps; tactical desktop ENet pass | Passing desktop; device validation required |
| Weighted zombie auto-aim | `AutoAimController2D`, `GameWorld` | Bounded candidates, distance/angle/threat scoring, 150-degree cone, fire-time LOS invalidation, hysteresis, smooth presentation, host fire direction; `AUTO AIM OK (46)` | Passing focused and desktop ENet; device validation required |
| Tower and trap combat | `TowerTargetingController2D`, `TacticalBuildSystem` | Three ranged towers plus blast, slow, and reusable root traps; host damage/status and replicated runtime/attack presentation; `TOWER COMBAT OK (70)` | Passing focused and desktop ENet; device validation required |
| Enemy identities | `EnemyPresentationCatalog`, `EnemyPresentation2D`, `ActorFacing` | Eight stable simulation variants mapped to one baked actor family with eight heading rows per clip, bake-derived pivots, uniform scale, distinct tints, silhouettes, markers, and motion cadence; `ENEMY PRESENTATION OK (55)` and a fresh Mobile/Vulkan contact sheet whose greyscale mass separates the crawler pack from the bulwark and the bosses | Passing desktop; device validation required |
| Base relocation | `TacticalBuildSystem` | All defenses cleared, per-resource floor(35%) refund, flow-mask move, zombie retarget | Passing solo and desktop ENet |
| Tactile feedback | `JuiceRig`, `AudioManager` | Exact 1.15x/0.25s pop, white flash restore, fixed eight-voice mono pool | Passing headlessly; device perception open |
| Projectile rendering | `player_projectile.tscn` | Authority vector, visible tracer, continuous collision, impact cleanup, and shoot cue tested | Passing |
| Action audio | `GameAudioController` | Valid shoot/build/harvest/UI streams, positional pool, UI player, and clean teardown tested | Passing headlessly; device audibility open |
| Impact camera | `CameraShake2D` | Bounded trauma, directional throw, exact return to rest, accessibility scale where 0 means untouched, and offset-only writes that never fight the focus tween; `CAMERA FEEDBACK OK (21)` | Passing desktop; device perception open |
| Damage response | `weather_overlay.gdshader`, `WeatherOverlay` | Edge-weighted hit wash composited into the one existing full-screen pass, strongest-wins stacking, exact decay to clear, and a zero scale that removes it outright; `DAMAGE FEEDBACK OK (35)` | Passing desktop; device perception open |
| Health readability | `HealthBar2D` | One drawn bar shared by survivors, Base Core, and horde, with chip drain, three threat bands carried by length and cadence as well as hue, no second Amber Gold focal source, a refuge bar proportioned against the camp's own alpha bounds, and a rest treatment that quiets the one readout that never hides - its bevel included, which is the element the treatment reached last and the one the gate could not see; `HEALTH READABILITY OK (76)` | Passing desktop; device validation required |
| Hit and death VFX | `VfxDirector`, `ActorGroundShadow2D` | Pooled muzzle, impact, and debris effects plus contact shadows whose radius and offset are both measured off each actor's own silhouette, all bounded and all free of simulation authority; `VFX DIRECTOR OK (154)` | Passing desktop; device profiling open |
| Enemy impact and health | `EnemyAgent2D` | Per-variant knockback mass, capped and clamped impulse, integration in the attack branch, root suppression through the movement multiplier, exact decay, and host/client health bars fed by the existing snapshot with no protocol change; `ENEMY IMPACT OK (42)` | Passing desktop; device validation required |
| Runtime asset audit | `deep_asset_audit.py`, `audit_and_extract_runtime_assets.py` | 31,404 files, 145 physical containers, 14 nested containers, 4,684 members, and 12 live promoted assets validated | Passing; subjective review remains limited to the promoted shortlist |
| Resource visuals | Three resource scenes | Required colors, identity rims at 1.4 logical pixels, local glow, and mipmapped icon delivery with the Wood anchor measured at 19.6 percent of body on delivered pixels | Complete |
| Character visuals | Heikki and Shane scenes | Blender RGBA sprites rendered in Godot | Complete |
| Poly Haven environment props | `polyhaven_environment_atlas.png`, `WorldObstacle2D` | `POLY HAVEN ENVIRONMENT VALIDATION OK (36 checks)`: six CC0 sources, twelve frames, provenance, atlas, materials, filtering, scale, no-3D, and retained collision | Integrated; physical Android profiling open |
| Poly Haven Hidden Alley district | `polyhaven_district_atlas.png`, `WorldObstacle2D`, `WorldBackgroundDecor2D` | `POLY HAVEN DISTRICT VALIDATION OK (58 checks)`: eight CC0 1k GLTF sources, eight frames, immutable workshop, atlas/luma/margins, exact 16 obstacle sprites, exact 75/46/34 furniture mix, export closure, no-3D, and retained collision | Integrated; final pixel refresh and physical Android profiling open |
| Local environment family | `local_environment_atlas.png`, `WorldObstacle2D` | `LOCAL ENVIRONMENT VALIDATION OK (48 checks)`, plus two source/license hash checks that run where the `Addons/` vault exists: sixteen frames from 29 GLBs, city/mall/village/roadside/camp composition, shared finish, the Ostari lot drawn in the lit concrete grain, and retained collision | Integrated; physical Android profiling open |
| Poly Haven terrain materials | `polyhaven_terrain_atlas.png`, biome mask, `terrain_material_blend.gdshader` | `TERRAIN MATERIAL VALIDATION OK (35 checks)`: four CC0 sources, organic blend, seamless sampling, lighting response, one 2D overlay, and no runtime 3D | Integrated; physical Android profiling open |
| Surface language | `surface_weathering.gdshader` | Rust, rot, and patina visible in GPU capture | Complete |
| Weather language | `weather_overlay.gdshader` | One non-intercepting screen pass rendered | Complete |
| Headless environment | `tests/smoke_test.gd` | `SMOKE OK (207 checks)`, run by CI on every push | Passing desktop and CI |
| Continuous integration | `tools/ci/run_gates.sh`, `.github/workflows/gates.yml` | Import, 22 headless gates, 3 ENet host/client probes, and 18 Mobile/Vulkan capture gates on Linux with Mesa lavapipe; 47/47 | Passing; not a device proof |
| Mobile/HUD geometry | `mobile_systems_validation.gd`, `tactical_hud_validation.gd` | `MOBILE SYSTEMS OK (58)` and `TACTICAL HUD OK (54)` cover safe area, measured chip-text fit, `44 x 44` targets, 50.9% dashboard area reduction, categories, pagination, and UI/world ownership | Passing desktop; physical touch validation required |
| GPU render gate | `tests/world_render_validation.tscn` | `captures=25` on Mobile/Vulkan, asserted to be the Mobile renderer and re-rendered by CI on every push | Passing desktop; Android device pixels open |
| Android export | `runtime_export_closure.json`, `runtime_export_dependencies.tscn` | Source closure declares 212 runtime resources; the dependency scene is generated from it with 213 strong dependencies and committed; the last PCK/54.29 MiB APK loaded 173 resources/23 scenes, passed the inventory gate with zero forbidden entries, and verified v2/v3 | Closure current; package predates the feedback stack and needs a rebuild before device install |
| Android device | Exported APK | No ADB device attached during validation | Not device-validated |
| Tactical world | `world_map_2d.tscn` | `WORLD MAP VALIDATION OK (226 checks)`: 14×14 grid, secluded `(9950, 2400)` camp, imported nature/camp/village coverage, terrain blend, chunked roads/decor, static collision, resolver, and flow mask, plus the forest understory, camp clearing, East Kylät grave plot, camp spur, and three road grades with one-surface junctions | Implemented; Android device gates open |
| World composition | `data/world/world_composition.tres`, `WorldCompositionContract` | Camp seat and satellites, nine graded road routes, biome paint, five landmarks, and thirteen pocket tables in one shipped Resource that `BesprenWorldMap2D` validates and consumes; a gate grep proves no world script authors a second copy; refactor proven output-identical against the capture noise floor | Passing desktop |
| Gameplay-zoom review | `tests/gameplay_tour_render_validation.tscn` | `GAMEPLAY TOUR RENDER OK \| zoom=0.38 \| captures=25`: camp, city, mall, both villages, the grave plot, roads by grade, forest, and wilderness at the shipped zoom with a survivor for scale | Passing desktop; Android device pixels open |
| Accessibility settings | `GameSettings`, `AccessibilitySettingsPanel`, `GameWorld.apply_settings` | Camera shake, damage flash, weather, and glow pulse in quarter steps, persisted to `user://settings.cfg`, 44 × 44 steppers, OFF at zero, each effect measured absent at zero; `SETTINGS LIFECYCLE OK (35)`, `START MENU LAYOUT OK (212)`, start-menu `captures=4` | Passing desktop; device validation required |
| Background lifecycle | `GameWorld._notification` | Focus loss and application pause release every touch in all modes; Solo pauses and resumes the tree and never strands its pause on exit; same gate | Solo passing desktop; LAN host grace window open |
| Resource gathering | `resource_scatter_2d.tscn` | Deterministic three-to-five interactions per node, intermediate zero yield, unchanged one-unit final yield, focus lock, revisions, and late-join progress; `RESOURCE SCATTER VALIDATION OK (72)` and desktop ENet pass | Passing desktop; physical two-device gate required |
| Gameplay mechanics | Movement, salvage, pacing, auto-aim, horde, and defense combat slice | Base deposit, persistent inventory, long-term progression, and release device certification remain open | Partial by design |
